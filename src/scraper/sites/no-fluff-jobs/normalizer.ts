import { JobOffer } from "@src/packages/offers/models";
import { normalize } from "path";
import { PostingDetail } from "./data-definitions";

export default function normalize(data: PostingDetail, url: url): JobOffer {
  const jobOffer: JobOffer = {
    url: url,
    title:
  }
}