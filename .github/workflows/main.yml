name: Main

on: [push, pull_request]

defaults:
  runs_on: ubuntu-latest
  uses: docker://node:14
jobs:
  build:
    steps:
      - run: "npm install"
      - run: "npm install gulp"
      - run: "gulp build"
  test:
    needs: build
    steps:
      - run: "npm install jest"
      - run: "jest"
